import React from "react";
import Button from "react-bootstrap/Button";

function AdoptButton() {
  return (
    <div>
      <Button variant="outline-primary">Click to Adopt</Button>
    </div>
  );
}

export default AdoptButton;
